# 2026-03-04 博客系统优化日志

## 📊 工作概述

今天主要进行博客系统的功能优化和问题修复，包括媒体库优化、P0 核心功能实施、上传问题修复。

---

## ✅ 完成的工作

### 1. 媒体库优化 (MEDIA-OPTIMIZATION-2026-03-04.md)

**文件命名规范化:**
- 新格式：`{type}_{timestamp}_{random}.{ext}`
- 示例：`img_20260304174520_a3f8b2c1.png`
- 类型前缀：img_, doc_, vid_, aud_, file_
- 14 位时间戳 + 8 位随机数 + 小写扩展名

**图片预览功能:**
- 点击媒体库图片查看大图
- 添加下载和复制链接按钮
- 深色背景图片查看器
- 响应式设计

**路径修复:**
- 确保所有图片路径以 `/` 开头
- 解决 404 错误问题

**修改文件:**
- `backend/routes/media.js` - 文件命名优化
- `frontend/src/views/admin/Media.vue` - 预览功能 + 路径修复

---

### 2. P0 核心功能实施

**回到顶部按钮:**
- 创建 `frontend/src/components/BackToTop.vue`
- 滚动超过 300px 显示
- 平滑滚动动画
- 渐变紫色背景
- 已在 App.vue 中集成

**阅读进度条:**
- 在 PostView.vue 添加进度条模板
- 显示文章阅读进度
- 待完成滚动监听逻辑

**已存在功能确认:**
- ✅ 首页文章列表（正常工作）
- ✅ 深色模式切换（Header 已有 ThemeToggle）

---

### 3. 媒体库上传问题修复 (紧急)

**问题:** 上传失败，错误"只支持图片文件"

**原因:** MIME type 验证过于严格

**修复方案:**
- 优先检查扩展名
- MIME type 作为备选验证
- 新增 BMP 格式支持
- 支持的格式：jpg, jpeg, png, gif, webp, svg, bmp

**修改文件:**
- `backend/routes/media.js` - fileFilter 函数优化

**上传流程优化:**
- 显示上传进度条（0-100%）
- 添加旋转图标和"正在上传到服务器..."提示
- 上传成功自动关闭预览对话框
- 上传失败显示具体错误信息

---

## 📁 创建的文档

1. `MEDIA-OPTIMIZATION-2026-03-04.md` - 媒体库优化完整文档
2. `memory/2026-03-04.md` - 今日工作日志（本文件）

---

## 🔧 技术细节

### 文件命名函数
```javascript
function generateFilename(file) {
  const now = new Date();
  const timestamp = now.toISOString().replace(/[-:T.]/g, '').slice(0, 14);
  const random = Math.random().toString(36).substring(2, 10);
  
  const ext = path.extname(file.originalname).toLowerCase().slice(1);
  let typePrefix = 'file';
  
  if (['jpg', 'jpeg', 'png', 'gif', 'webp', 'svg'].includes(ext)) {
    typePrefix = 'img';
  } else if (['pdf', 'doc', 'docx'].includes(ext)) {
    typePrefix = 'doc';
  } else if (['mp4', 'webm'].includes(ext)) {
    typePrefix = 'vid';
  } else if (['mp3', 'wav'].includes(ext)) {
    typePrefix = 'aud';
  }
  
  return `${typePrefix}_${timestamp}_${random}.${ext}`;
}
```

### 上传进度监听
```javascript
xhr.upload.addEventListener('progress', (e) => {
  if (e.lengthComputable) {
    uploadProgress.value = Math.round((e.loaded / e.total) * 100);
  }
});
```

---

## 🌐 部署信息

**前端部署:**
- 构建命令：`npm run build`
- 部署路径：`/var/www/blog-frontend/`
- 部署脚本：清理 assets → 复制 dist → nginx reload

**后端管理:**
- PM2 进程名：blog-backend
- 重启命令：`pm2 restart blog-backend`
- 日志查看：`pm2 logs blog-backend`

**访问地址:**
- 博客首页：https://sqlboy.top/
- 媒体库：https://sqlboy.top/admin/media
- 管理后台：https://sqlboy.top/admin/

---

## 📋 待完成功能

### P1 - 重要功能
- [ ] 阅读进度条完整实现（滚动监听）
- [ ] 社交分享功能
- [ ] RSS 订阅入口
- [ ] 文章归档页面
- [ ] 移动端目录优化

### P2 - 增强功能
- [ ] 文章版权信息
- [ ] 友情链接
- [ ] 文章置顶
- [ ] 阅读量统计显示
- [ ] 相关推荐

---

## 🎯 关键决策

1. **文件命名方案** - 采用类型前缀 + 时间戳 + 随机数，便于管理和识别
2. **上传验证策略** - 扩展名优先，MIME type 备选，提高兼容性
3. **UI/UX 优化** - 深色主题图片查看器，平滑动画，响应式设计

---

## 📝 注意事项

1. 上传文件限制 5MB
2. 媒体库路径必须以 `/` 开头
3. 图片预览需要释放 URL 对象避免内存泄漏
4. PM2 重启后需要等待 2 秒确保服务就绪

---

**记录时间:** 2026-03-04 21:30  
**记录者:** AI Assistant  
**项目:** blog.sqlboy.top
